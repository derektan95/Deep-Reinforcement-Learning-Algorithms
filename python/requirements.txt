tensorflow==1.7.1
Pillow>=4.2.1
matplotlib
numpy>=1.11.0
jupyter
pytest>=3.2.2
docopt
pyyaml
protobuf==3.5.2
grpcio==1.11.0
pandas
scipy
ipykernel

# NOTE: You may need to manually install latest pytorch using `pip install torch` to prevent weird installation errors (Manually installing 0.4.0 may causes jupyter notebook hanging error). 
#       Otherwise, if you have CUDA, please refer to `https://pytorch.org/get-started/locally/`. DO NOT install both torch and pytorch, becayse conda views them separately.
#torch==0.4.0   
